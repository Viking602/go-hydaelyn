# v0.15.2

## Host-owned context transitions

- Add `agent.ContextTransition` as an optional provider-neutral engine extension.
- Invoke it after a completed tool batch has been appended and before the next model boundary.
- Clone transition inputs and outputs so host checkpoint/rewind implementations cannot alias engine history.
- Preserve normal step recording, usage accounting, streaming, and turn-control semantics around the transition.
