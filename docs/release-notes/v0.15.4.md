# v0.15.4

## MCP resource-template discovery

- Add `mcpcontract.ResourceTemplateClient` and the typed `mcpcontract.ResourceTemplate` record.
- Implement `ListResourceTemplates` on the MCP client alongside resources, prompts, notifications, and subscriptions.
- Map SDK resource templates losslessly: URI template, name, description, and MIME type.
